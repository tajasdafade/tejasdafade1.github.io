function addToCart(productName, price) {
    alert(`Added ${productName} to cart. Total Price: $${price}`);
}
// JavaScript function to handle adding products to the cart
function addToCart(productName, price) {
    // This is a simplified example, you might want to implement a proper cart system
    alert(`Added ${productName} to the cart. Price: $${price}`);
}

// You can add more JavaScript functions or logic as needed
